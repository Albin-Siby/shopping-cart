<div class="row">
    <div class="col-md-6 m-auto p-5">
        <div class="container mt-5 pl-5">
            <h2 class="text-center">Login</h2>
            <form action="/login" method="post">

                <div class="form-group mt-5">
                    <label><strong>Email address</strong></label>
                    <input type="email" class="form-control" name="Email" value="{{values.Email}}" placeholder="Enter email">    
                </div>

                <div class="form-group mt-3">
                    <label><strong>Password</strong></label>
                    <input type="password" class="form-control" name="Password" value="{{ values.Password}}" placeholder="Password">
                </div>

                {{#if loginError}}
                {{#each loginError}}
                    <p class="mt-3" id="error" style="color: red;">{{msg}}</p>
                {{/each}}
                {{/if}}

                {{#if loginErr}}
                    <p class="mt-3" id="error" style="color: red;">{{loginErr}}</p>
                {{/if}}

                <div class="mt-5">
                    <a href="/signup">Create new account ?</a>
                <button type="submit" class="btn btn-primary float-end">Login</button>
                </div>
            </form>
        </div>
    </div>
</div> 


{{!-- <style>

form {
    text-align: center;
}

.login {
    width: 400px;
    font-size: 16px;
    position: absolute;
    left: 50%;
    top: 50%;
    box-shadow: 0 5px 6px #00000036;
    transform: translate(-50%, -50%);
    border-radius: 22px;
    overflow: hidden;
    transition: 1s;
    height: 430px;
}

.login-header, input {
    margin-top: 0;
    margin-bottom: 0;
}

.input-container {
    opacity: 1;
    transition: 1s;
}

img {
    width: 80px;
    border-radius: 50%;
    padding: 12px;
    box-shadow: 0 2px 6px #001a348a;
}

.login-header {
    background: rgb(0, 55, 109);
    padding: 20px;
    font-size: 1.4em;
    font-weight: normal;
    text-align: center;
    text-transform: uppercase;
    color: #fff;
    padding-top: 32px;
}

.login-container {
    background: rgb(0, 55, 109);
    padding: 40px 30px;
    transition: 1s;
}

.login .value {
    padding: 12px;
}

.login input {
    box-sizing: border-box;
    display: block;
    width: 100%;
    border-width: 1px;
    border-style: solid;
    padding: 16px;
    outline: 0;
    font-family: inherit;
    font-size: 0.95em;
}

.login input[type="email"],
.login input[type="password"] {
    background: #fff;
    border-color: #bbb;
    color: rgb(170, 170, 170);
    border: none;
    background-color: transparent;
    border-bottom: 1px solid #c0c0c03b;
    padding-left: 0;
    padding-bottom: 10px;
}

.login input[type="email"]:focus,
.login input[type="password"]:focus {
    border-color: #888;
}

.login input[type="button"] {
    background: rgb(126, 144, 161);
    width: 200px;
    margin: auto;
    border-bottom: 22px;
    border: none;
    color: rgb(12, 12, 12);
    overflow: hidden;
    border-radius: 22px;
    cursor: pointer;
    margin-top: 12px;
}

.login input[type="button"]:hover {
    background: black;
    color: white;
}

.hide-section {
    opacity: 0;
}

.hide-form {
    padding: 0;
    padding-top: 27px;
}

.hide-login {
    width: 165px;
    height: 167px;
    border-radius: 50%;
}
</style>

 <div class="login hide-login">

        <form class="login-container hide-form" action="login" method="post" autocomplete="off">
            
            <img src="https://png.pngtree.com/png-vector/20191003/ourmid/pngtree-user-login-or-authenticate-icon-on-gray-background-flat-icon-ve-png-image_1786166.jpg" alt="" title="Click here..." />
            
            <div class="input-container hide-section">
                <h3>Login</h3>
                
                <div class="value">
                    <input placeholder="Email" type="email" name="email" required>
                </div>

                <div class="value">
                    <input placeholder="Password" type="password" name="password" required>
                </div>

                <div class="value">
                    <input type="button" value="Log in">
                </div>

            </div>
        
        </form>
    
    </div>


    <script>
        const section = document.querySelector(".hide-section")
const form = document.querySelector(".login-container")
const login = document.querySelector(".login")
const profile = document.querySelector("img")

profile.onclick = () => {
    requestAnimationFrame(() => {
        login.classList.remove("hide-login");
        form.classList.remove("hide-form");

        setTimeout(() => {
            section.classList.remove("hide-section");
        },1000)
    });
};
    </script> --}}